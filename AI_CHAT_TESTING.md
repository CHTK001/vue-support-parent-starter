# AI 聊天助手测试指南

## 快速测试步骤

### 1. 启用 AI 助手

1. 启动应用
2. 点击右上角的设置图标
3. 找到 "AI 助手" 设置区域
4. 开启 "启用 AI 助手" 开关
5. 页面右下角应该出现一个紫色的机器人图标

### 2. 测试基本对话

1. 点击机器人图标打开聊天窗口
2. 在输入框中输入：`你好`
3. 按回车或点击发送按钮
4. 等待 AI 回复（首次使用可能需要 20 秒加载模型）

### 3. 测试中文对话

测试问题示例：

```
1. 你好，请介绍一下自己
2. 什么是 Vue.js？
3. 如何学习编程？
4. 给我讲个笑话
5. 帮我写一个 JavaScript 函数来计算斐波那契数列
```

### 4. 测试英文对话

测试问题示例：

```
1. Hello, who are you?
2. What is TypeScript?
3. How to learn web development?
4. Tell me a joke
5. Write a Python function to sort an array
```

### 5. 测试皮肤切换

1. 在系统设置中找到 "机器人皮肤" 选项
2. 依次选择不同的皮肤：
   - 🤖 机器人
   - 🦊 阿狸
   - 🐱 猫咪
   - 🐻 小熊
   - 🐼 熊猫
3. 观察聊天窗口中的图标是否变化

### 6. 测试主题切换

1. 在系统设置中找到 "AI 助手主题" 选项
2. 依次选择不同的主题：
   - Default（紫色）
   - Blue（蓝色）
   - Green（绿色）
   - Orange（橙色）
   - Pink（粉色）
   - Dark（深色）
3. 观察机器人图标和聊天窗口的颜色是否变化

### 7. 测试位置切换

1. 在系统设置中找到 "机器人位置" 选项
2. 依次选择不同的位置：
   - 右下角
   - 左下角
   - 底部居中
3. 观察机器人图标的位置是否变化

### 8. 测试窗口拖拽

1. 打开聊天窗口
2. 点击并按住窗口顶部的标题栏
3. 拖动鼠标移动窗口
4. 释放鼠标，窗口应该停留在新位置

### 9. 测试错误处理

#### 测试速率限制

1. 快速连续发送多条消息（10 条以上）
2. 观察是否出现 429 错误提示
3. 等待一段时间后重试

#### 测试网络错误

1. 断开网络连接
2. 发送一条消息
3. 观察是否出现网络错误提示
4. 重新连接网络后重试

### 10. 测试自定义 API

#### 使用 Hugging Face API Key

1. 访问 https://huggingface.co/
2. 注册并获取 API Token
3. 在系统设置中输入 API Key
4. 发送消息测试是否正常工作

#### 使用其他模型

1. 在 API URL 中输入：
   ```
   https://api-inference.huggingface.co/models/mistralai/Mistral-7B-Instruct-v0.2
   ```
2. 发送英文消息测试
3. 观察回复质量

## 预期结果

### 正常情况

- ✅ 机器人图标正常显示
- ✅ 聊天窗口可以打开和关闭
- ✅ 消息可以正常发送和接收
- ✅ AI 回复内容相关且合理
- ✅ 皮肤和主题切换正常
- ✅ 窗口可以拖拽移动
- ✅ 错误提示清晰友好

### 首次使用

- ⏳ 首次发送消息需要等待 20 秒左右
- ⏳ 可能看到 503 错误提示（模型加载中）
- ✅ 等待后重试应该成功

### 速率限制

- ⚠️ 快速连续请求可能触发 429 错误
- ✅ 错误提示应该说明需要等待
- ✅ 等待后重试应该成功

## 常见问题

### Q1: 首次使用一直加载怎么办？

**A**: 这是正常现象，Hugging Face 免费 API 需要冷启动模型。请等待 20-30 秒后重试。

### Q2: 回复内容不相关怎么办？

**A**:

1. 尝试更清晰地描述问题
2. 使用更具体的问题
3. 尝试切换到其他模型

### Q3: 经常出现 429 错误怎么办？

**A**:

1. 减少请求频率
2. 配置自己的 API Key
3. 等待一段时间后重试

### Q4: 如何测试代码生成功能？

**A**: 可以尝试以下问题：

```
1. 写一个 Vue 3 组件示例
2. 如何使用 TypeScript 定义接口？
3. 帮我写一个排序算法
4. 如何使用 fetch API 发送请求？
```

## 性能测试

### 响应时间

- 首次请求：15-30 秒（模型加载）
- 后续请求：2-5 秒（正常响应）
- 网络良好时：1-3 秒

### 并发测试

- 单用户连续请求：建议间隔 2 秒以上
- 多用户同时使用：免费 API 有全局速率限制

### 内存占用

- 聊天窗口关闭时：几乎无额外内存占用
- 聊天窗口打开时：约 5-10 MB
- 长时间对话：建议定期刷新页面清理历史

## 测试检查清单

- [ ] AI 助手可以正常启用
- [ ] 机器人图标正常显示
- [ ] 聊天窗口可以打开和关闭
- [ ] 可以发送和接收消息
- [ ] 中文对话正常
- [ ] 英文对话正常
- [ ] 代码生成功能正常
- [ ] 5 种皮肤都可以正常切换
- [ ] 6 种主题都可以正常切换
- [ ] 3 种位置都可以正常切换
- [ ] 窗口可以拖拽移动
- [ ] 错误提示清晰友好
- [ ] 首次使用提示明确
- [ ] API Key 配置功能正常
- [ ] 自定义 API URL 功能正常
- [ ] 配置保存和恢复正常

## 自动化测试建议

如需编写自动化测试，可以测试以下方面：

### 单元测试

```typescript
// 测试消息发送
test("should send message", async () => {
  const component = mount(LayAiChat);
  await component.vm.sendMessage("Hello");
  expect(component.vm.messages.length).toBeGreaterThan(1);
});

// 测试皮肤切换
test("should change skin", async () => {
  const component = mount(LayAiChat);
  component.vm.skin = "fox";
  expect(component.vm.currentSkinIcon).toBe(FoxIcon);
});
```

### 集成测试

```typescript
// 测试 API 调用
test("should call API correctly", async () => {
  const mockFetch = jest.fn();
  global.fetch = mockFetch;

  const component = mount(LayAiChat);
  await component.vm.sendMessage("Test");

  expect(mockFetch).toHaveBeenCalledWith(
    expect.stringContaining("huggingface.co"),
    expect.any(Object),
  );
});
```

### E2E 测试

```typescript
// 使用 Playwright 或 Cypress
test("complete chat flow", async ({ page }) => {
  await page.goto("/");
  await page.click(".ai-bot-trigger");
  await page.fill('input[type="text"]', "Hello");
  await page.click(".send-btn");
  await page.waitForSelector(".message.assistant");
  expect(await page.textContent(".message.assistant")).toBeTruthy();
});
```

## 测试报告模板

```markdown
# AI 聊天助手测试报告

## 测试信息

- 测试日期：2026-02-26
- 测试人员：[姓名]
- 测试环境：[浏览器/操作系统]
- 应用版本：v1.0.0

## 测试结果

### 功能测试

| 功能         | 状态 | 备注             |
| ------------ | ---- | ---------------- |
| 启用 AI 助手 | ✅   | 正常             |
| 发送消息     | ✅   | 正常             |
| 接收回复     | ✅   | 首次需等待 20 秒 |
| 皮肤切换     | ✅   | 正常             |
| 主题切换     | ✅   | 正常             |
| 位置切换     | ✅   | 正常             |
| 窗口拖拽     | ✅   | 正常             |
| 错误处理     | ✅   | 提示清晰         |

### 性能测试

| 指标         | 结果  | 标准    |
| ------------ | ----- | ------- |
| 首次响应时间 | 22 秒 | < 30 秒 |
| 后续响应时间 | 3 秒  | < 5 秒  |
| 内存占用     | 8 MB  | < 20 MB |

### 问题记录

1. [问题描述]
2. [问题描述]

### 改进建议

1. [建议内容]
2. [建议内容]

## 总体评价

[测试总结]
```

---

**祝测试顺利！** 🧪✨
