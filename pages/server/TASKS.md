# Server 31 Tasks

更新时间: `2026-04-10`

## 总览

1. `[已完成]` 修复预警中心最新预警排序与滚动锚点。
2. `[已完成]` 重做指标卡片与指标详情，修复采集时间与历史展示，统一 `SPI(LOCAL/OSHI, SSH, WINRM)`。
3. `[已完成]` 补齐服务管理手工新增、自动检测与系统能力回退。
4. `[已完成]` 把 AI 稳定性分析收拢到预警中心并改成图标化入口。
5. `[已完成]` 调整基础信息高度和布局，去掉无意义滚动条。
6. `[已完成]` 收口服务器编辑表单默认值、本机自动获取、数字输入框高度。
7. `[已完成]` 完善预警中心最新时间、间距与详情弹框。
8. `[已完成]` 完善服务区布局、详情弹框与脚本编辑组件。
9. `[已完成]` 收口远程代理接入并保留兼容回退，默认改为右侧覆盖面板。
10. `[已完成]` 实现 TechUI 服务器大屏真实数据版。
11. `[已完成]` 实现服务器聚合多选与聚合大屏真实数据版。
12. `[已完成]` 文件管理支持列表/树状、相对路径、文本编辑同步与 SPI 抽象。
13. `[已完成]` 取消第三列强制等高，改为自适应布局。
14. `[已完成]` 公网地址统一通过 `ifconfig.me` 获取。
15. `[已完成]` 暴露指标采集定时任务配置并支持启停。
16. `[已完成]` 排查并修复 AI 能力未激活问题，页面增加诊断状态与明细。
17. `[已完成]` 为项目管理和进程管理统一卡片与详情弹框范式。
18. `[已完成]` 移除服务器初始化数据依赖，仅保留本机和测试服务器。
19. `[已完成]` 新增顶部任务按钮与统一任务进度组件。
20. `[已完成]` 新增项目管理模块并接入现有 soft/server-service/AI 能力。
21. `[已完成]` 集成 `ai-starter` 到服务启动失败分析/修复/知识库沉淀，并补前端展示。
22. `[已完成]` 全局 `--el-dialog-margin-top` 默认值改为 `2vh`。
23. `[已完成]` 修复服务器编辑、预警设置、远程代理排版并抽服务器管理布局，受控表单已补齐。
24. `[已完成]` 排查网络 IO 无数据并补真实指标历史表，指标任务已接入 `job-starter`。
25. `[待验证]` 测试服务器 SSH/Docker 环境已确认，但当前 `CentOS7 + Docker 20.10.0` 对 Guacamole 官方容器存在运行时阻塞，待切换部署路径并形成可复验记录。
26. `[待验证]` 已补 AI 集成回归清单与回归记录模板，待真实联调并形成测试记录。
27. `[已完成]` 新增进程管理模块与历史查询/详情弹框。
28. `[已完成]` 新增服务器进程查看按钮，支持实时推送、AI 分析、关闭等能力。
29. `[已完成]` 把网络指标主展示从包速率改成 MB 吞吐展示。
30. `[已完成]` 为指标补齐历史查询能力。
31. `[已完成]` 为每个卡片补齐详情弹框入口与交互。

## 明细

1. 修复预警中心“最新预警不在最上面、视图自动跳到底部”的问题，统一首屏展示与新告警滚动锚点。
2. 重做指标卡片与指标详情，保证卡片主体突出、采集时间正确、历史 X 轴可信、磁盘分区用阈值色进度条展示，并把指标获取统一成 `SPI(LOCAL/OSHI, SSH, WINRM)`。
3. 补齐服务管理的手工新增、自动检测和系统能力回退逻辑，自动检测必须优先走 `SPI(LOCAL/SSH/WINRM)`，有 AI 用 AI，没有 AI 用操作系统能力。
4. 把“AI 稳定性分析”作为服务器详情核心动作收拢到预警中心，尽量改为图标 + tooltip，并让 AI 给出“当前时间”的系统稳定性结论。
5. 调整基础信息卡片高度和布局，去掉无意义滚动条，保证信息区自然铺开。
6. 收口服务器编辑/新增表单细节，统一数字输入框高度；`SSH` 默认端口 `22`、根目录默认 `/`；`LOCAL` 基本信息自动获取，不要求手填；远程服务器统一走 `SPI(LOCAL/SSH/WINRM)` 获取默认值。
7. 完善预警中心：触发预警时显示最新时间，拉开 header 与“最新预警”间距，并补预警详情弹框，支持查看触发快照。
8. 完善服务器服务区：压缩上下间距，把输入框和类型放一行；补服务详情弹框；把脚本编辑抽成新组件，支持常用软件快捷模板、AI 生成和 `SPI(LOCAL/SSH/WINRM)` 执行。
9. 收口远程代理接入，保留兼容网关实现，前端远程打开默认覆盖右侧并支持全屏，后端统一接入远程网关配置。
10. 使用 `TechUI` 实现服务器大屏页面；现阶段允许先做空白占位页，把整体任务写在页面中。
11. 在服务器列表左侧增加聚合按钮和多选模式，支持打开聚合大屏；现阶段允许先做空白聚合页并写明整体任务。
12. 文件管理支持列表/树状两种展示，右侧文本可编辑且修改后出现同步按钮，路径显示相对路径，上传/下载/删除/预览统一确认并抽象为 `SPI(LOCAL/SSH/WINRM)`。
13. 取消第三列“必须等高”的硬性布局，改成以视觉效果优先的自适应高度或瀑布感布局。
14. 公网地址统一通过 `ifconfig.me` 获取，并补本机/SSH/WinRM 的异常回退和联调测试。
15. 把指标获取定时任务配置暴露到服务器页，支持修改 cron/间隔、启停任务，便于直接在页面调整采集策略。
16. 排查并修复“AI 能力没有激活”的问题，同时把未激活原因明确展示到页面上。
17. 为项目管理和进程管理预先统一一套卡片 + 详情弹框范式，所有卡片支持 AI 和 `SPI(LOCAL/SSH/WINRM)`，保证后续两个模块 UI/交互一致。
18. 移除服务器初始化数据依赖，默认不把服务器当初始化数据，只保留当前联调用的本机和测试服务器。
19. 顶部新增任务按钮，并在 `lay-setting` 加开关；实现统一任务进度组件，支持纯进度、实时消息 + 进度、标题自定义、位置自定义、自动叠放、数量限制、自动/手动关闭、百分比等，所有 socket 任务统一接入。
20. 新增项目管理模块，重点支持 Spring Boot 单项目、前后端分离、Nginx 静态页，支持启动/停止/备份/还原脚本、页面操作和实时日志。
21. 集成 `ai-starter` 到服务启动失败流程：失败后把服务器信息与日志给 AI 分析，结果写入本次启动记录并回前端；前端在最新失败场景显示“AI 修复启动”与“普通启动”双按钮；失败原因、方案和修复记录沉淀到知识库表。
22. 把 `--el-dialog-margin-top` 全局默认值改成 `2vh`，不要只在个别弹框单独写。
23. 修复服务器编辑、预警设置、远程代理三类弹框的排版问题，并为服务器管理设计新的卡片/弹框布局，后续复用到远程代理、预警等。
24. 排查网络 IO 一直没有数据的问题；确定指标/历史是否落 `job-starter` 自定义表前缀表；把所有定时采集统一收敛到 SPI + job 架构。
25. 在测试服务器上用 Docker 安装远程代理，形成最终远程代理方案的真实测试环境。
26. 对 AI 集成做完整联调测试，覆盖服务启动失败分析、服务脚本、项目启动失败分析、报告分析、脚本生成等场景，并形成可复验记录。
27. 新增进程管理模块，统一走 `SPI(LOCAL/SSH/WINRM)`，每个卡片支持详情弹框，指标弹框支持历史查询。
28. 新增服务器进程查看按钮，支持实时推送、AI 分析、关闭进程等能力，作为进程管理模块的快捷入口。
29. 把网络指标主展示从包速率改成 MB 吞吐展示，统一卡片、图表和详情弹框的单位与文案。
30. 为指标补齐历史查询能力，支持按时间范围查看历史曲线与明细。
31. 为每个卡片补齐详情弹框入口与交互，保证卡片点击即可打开对应详情。

## 当前主线

- 当前正在推进: `25 Docker 远程代理测试`, `26 AI 联调测试`
- 当前已落地: `9`, `10`, `11`, `14`, `16`, `19`, `20`, `21`, `22`, `29`, `30`
- 本轮新增落地: `SERVER_PROCESS` Socket 事件、进程实时流 composable、进程弹框 Socket 在线态、AI 能力诊断字段、左侧/右键进程入口、项目页统计卡片下钻、指标采集任务 `job-starter` 化与任务表状态回显、远程代理弹框改为受控表单并通过 eslint、远程控制面板补齐全屏状态同步与新标签打开入口、基础信息/预警卡片去内部滚动条并补 AI 诊断明细、服务器编辑和预警设置改为受控表单、AI 回归记录模板、通用 `ScAiTextarea` AI 编辑器、`ScInput` 文本/多行/密码样式统一、服务脚本与配置模板 AI 化、服务器表单/指标任务/侧栏搜索/进程搜索/项目搜索/标签输入/安装引导输入框统一、AI 未激活时可从服务器页直达系统设置面板、指标详情图表补齐秒级/毫秒级时间轴与样本数展示、网络指标补齐 `总吞吐/入站/出站` 三线趋势、网络明细改为累计流量优先展示、AI 未激活入口继续收口为图标 + tooltip、预警中心新增“最新触发”快捷详情卡、远程代理弹框压缩 provider/protocol 选择布局、预警设置弹框改成更紧凑的 2 列阈值排版、服务器编辑弹框补齐接入/默认值摘要、主面板改成“基础信息 + 预警/服务堆叠”布局以减少第三列等高感、单机大屏接入真实指标/告警/服务数据、聚合大屏接入多机指标对比与聚合告警/服务分布、服务详情弹框补齐运行摘要/脚本预览/AI 修复脚本、服务自动检测补齐 `SPI 通道/检测来源/检测时间` 元数据并回显到卡片与详情弹框、远程网关设置支持读取 legacy `guacamole*` 元数据并按默认 provider 回退、前端远程控制配置类型统一为 `ServerRemoteConsoleConfig`、项目管理页补齐 AI 未激活原因代码/Provider 解析来源/系统设置入口、`@pages/server` 新增 `build.config.ts` 并恢复包级构建通过、后端主档补齐默认根目录兜底
- 参考效果图对应任务: `19 统一任务进度组件`
