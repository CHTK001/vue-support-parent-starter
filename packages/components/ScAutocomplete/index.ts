import { withInstall } from "@repo/utils";
import ScAutocomplete from "./src/index.vue";

export const ScAutocompleteComponent = withInstall(ScAutocomplete);

export { ScAutocomplete };
export default ScAutocompleteComponent;
