# vue-support-parent 样式整理与体积瘦身清单（2026-04-13）

## 1. 目标

把当前前端样式按下面三层重新收敛：

1. `layout/`：只放布局框架、主题壳、全局容器、导航、响应式骨架、主题皮肤。
2. `apps/<app>/src/styles`：只放某个应用自己的视觉样式，不再复制公共 token / mixin / reset / layout 规则。
3. `packages/assets`：只放跨应用复用的公共样式资产，例如 token、mixins、reset、公共 theme override、公共 page utilities。

同时解决 4 个问题：

- 删除 layout 与 assets 的双份样式副本
- 删除 app 之间重复的 variables/mixins/common 样式
- 降低 turbo/workspace 下“全量感知、全量牵连”的构建成本
- 让大组件像 `components-standalone` 一样按需独立，不把整仓样式和依赖一起带入

---

## 2. 我当前对仓库的理解

### 2.1 当前样式结构

仓库里现在样式主要分布在：

- `layout/default/src/**`
- `packages/assets/**`
- `apps/**/src/styles/**`
- `pages/**`（少量页面样式 + 一个已产物化的 `dist-standalone` CSS）
- `packages/components*` / `packages/standalone`（部分大组件自己的样式）

### 2.2 当前样式统计（源码层）

- 总样式文件数：`206`
- `layout`：`89` 个，约 `552.85 KB`
- `packages/assets`：`49` 个，约 `245.77 KB`
- `apps`：`38` 个，约 `325.03 KB`
- `pages`：`7` 个，约 `418.57 KB`
- `packages/components`：`7` 个，约 `39.39 KB`
- `packages/components-standalone`：`7` 个，约 `60.52 KB`
- `packages/standalone`：`7` 个，约 `60.52 KB`

> `pages/doc/dist-standalone/doc-v2-style.css` 单文件约 `381 KB`，明显属于构建产物，不应该长期作为源码样式参与 workspace 体积感知。

### 2.3 当前最核心的问题

#### 问题 A：`layout/default/src/styles` 与 `packages/assets/styles` 并存双份副本

已确认 **25 组完全相同** 的样式文件，重复源文件约 **110 KB**，包括：

- `layout/default/src/styles/layout/*` ↔ `packages/assets/styles/layout/default/*`
- `layout/default/src/styles/colors/*` ↔ `packages/assets/styles/colors/*`
- `layout/default/src/styles/theme-skin/*` ↔ `packages/assets/styles/layout/theme-skin/*`
- `layout/default/src/styles/font-encryption.scss` ↔ `packages/assets/style/font-encryption.scss`
- `layout/default/src/styles/modern-theme.scss` ↔ `packages/assets/style/legacy/modern-theme.scss`
- `layout/default/src/styles/layout-index.scss` ↔ `packages/assets/style/legacy/layout-index.scss`

也就是说：

- 仓库已经在“迁移到 `packages/assets`”
- 但迁移没收口，导致 layout 里还留着一整套旧实现
- 构建入口还在混用新旧路径

#### 问题 B：`packages/assets` 自己又同时存在 `style/` 和 `styles/`

这说明 `assets` 包内部也没彻底收敛：

- README 说“统一放 `style/`”
- 但当前实际被引用的是 `@repo/assets/styles/...`
- `style/` 与 `styles/` 又存在兼容入口 + 平行实现

这会带来：

- 新人不知道该从哪里引
- 一次改样式需要同时维护两到三份路径
- tree-shaking 很难建立明确边界

#### 问题 C：构建默认注入还绑着 `@layout/default`

`packages/build-config/src/standard-config.ts` 的 `scss.additionalData` 仍然是：

- `@layout/default/styles/layout/variables.scss`
- `@layout/default/styles/layout/mixin.scss`

这意味着：

- 即使 app 只想用公共 token，也被强耦合到 layout 包
- layout 改动会导致大量 app 的样式预处理链重新受影响
- `packages/assets` 很难成为真正唯一公共入口

#### 问题 D：app 之间在复制“公共 mixins / variables”

已确认以下 app 的样式文件存在明显语义重复：

- `apps/vue-support-payment-starter/src/styles/mixins.scss`
- `apps/vue-support-sync-starter/src/styles/mixins.scss`
- `apps/vue-support-swagger-starter/src/styles/mixins.scss`
- `apps/vue-support-hotspot-starter/src/styles/mixins.scss`
- `apps/vue-support-monitor-starter/src/styles/mixins.scss`

其中前四者几乎是同一份公共 mixin 模板的拷贝；monitor 版本更大，但其中大量基础 flex / text / card / responsive mixin 也属于公共层。

#### 问题 E：standalone / components-standalone / components 存在三套大组件样式镜像

已确认以下组件样式是重复副本：

- `ScMap`
- `ScLayer`

分布于：

- `packages/components/*`
- `packages/components-standalone/*`
- `packages/standalone/*`

这会造成：

- 样式维护三处同步
- 大组件边界不清晰
- workspace 下依赖图变大

---

## 3. 目标结构（建议落地方案）

### 3.1 最终职责划分

#### A. `layout/`

只保留：

- 布局壳体：header / sidebar / tabs / drawer shell / container
- 布局主题皮肤：dark / christmas / halloween / future-tech / skin
- 布局行为相关样式：响应式骨架、布局切换动画、导航模式差异
- 只被 layout 组件内部消费的局部样式

不再保留：

- 通用 reset
- 通用 variables / mixins
- 通用 color token
- 通用 element-plus override
- 所有 app 都可复用的 theme utilities

#### B. `packages/assets`

作为唯一公共样式源，建议保留：

- `tokens/`：颜色、圆角、阴影、spacing、z-index、字体等 token
- `mixins/`：flex / text / card / scrollbar / responsive / glass 等通用 mixin
- `base/`：reset、tailwind bridge、基础 html/body/scrollbar、font-encryption
- `theme/`：公共颜色主题、Element Plus override、theme-skin 公共层
- `pages/`：跨应用可复用页面级外观（例如 `modern-page`）
- `legacy/`：仅保留非常少量兼容转发文件，且必须有淘汰时间

#### C. `apps/<app>/src/styles`

只保留：

- 应用品牌色微调
- 应用首页/控制台/专题页视觉样式
- 应用特有的页面布局差异
- 不可抽象到公共层的业务样式

不再保留：

- 公共 variables
- 公共 mixins
- 公共 reset
- 从别的 app 复制来的 card / form / toolbar / flex 规则

---

## 4. 建议的目录重组清单

## 4.1 `packages/assets` 建议收口为唯一源

建议重组为：

```text
packages/assets/
  fonts/
  images/
  svg/
  styles/
    base/
      reset.scss
      tailwind.css
      font-encryption.scss
    tokens/
      colors.scss
      spacing.scss
      radius.scss
      shadow.scss
      z-index.scss
      motion.scss
      index.scss
    mixins/
      layout.scss
      text.scss
      card.scss
      effects.scss
      responsive.scss
      index.scss
    theme/
      colors/
      element-plus/
      skin/
      stitch/
      index.scss
    pages/
      modern-page.scss
    layout/
      default-entry.scss
    legacy/
      ...（只保留过渡转发）
```

### 4.2 `layout/default` 建议保留

建议保留：

- `src/components/**/themes`
- `src/skin/**`
- `src/themes/**`
- `src/styles/layout.scss` / `layout-index.scss` 中真正属于 layout 壳的内容

建议迁出或删除：

- `src/styles/layout/*` 中公共 token / reset / mixin / element-plus override
- `src/styles/colors/*`
- `src/styles/theme-skin/*`（如果是公共主题层则迁到 assets）
- `src/styles/font-encryption.scss`
- `src/styles/modern-theme.scss`
- 与 `packages/assets` 完全一致的兼容副本

### 4.3 apps 样式目录建议标准化

每个 app 统一成：

```text
apps/<app>/src/styles/
  app.scss           # app 唯一入口
  brand.scss         # app 品牌变量（少量）
  pages/
  components/
```

删除：

- `variables.scss`（如果只是公共 token 的复制）
- `mixins.scss`（如果只是公共 mixin 的复制）
- 从 monitor/payout/sync/swagger/hotspot 之间互相复制的模板文件

---

## 5. 具体清理清单

### 第一组：立即可删/可迁移的重复公共样式

1. 对比删除 `layout/default/src/styles/layout/*` 与 `packages/assets/styles/layout/default/*` 的双份文件
2. 对比删除 `layout/default/src/styles/colors/*` 与 `packages/assets/styles/colors/*` 的双份文件
3. 对比删除 `layout/default/src/styles/theme-skin/*` 与 `packages/assets/styles/layout/theme-skin/*` 的双份文件
4. 对比删除 `layout/default/src/styles/font-encryption.scss`
5. 对比删除 `layout/default/src/styles/modern-theme.scss`
6. 对比删除 `layout/default/src/styles/layout-index.scss` 的 legacy 镜像来源

### 第二组：立即可抽公共的 app 样式

1. 抽离四个 app 共用的 `flex-* / text-ellipsis / card-*` mixin 到 `packages/assets/styles/mixins/`
2. 抽离四个 app 共用的 `radius/shadow/border/duration/ease/gradient` 变量到 `packages/assets/styles/tokens/`
3. monitor 的大份 `mixins.scss` 拆成：
   - 公共 mixins → `packages/assets`
   - monitor 专属 mixins → `apps/vue-support-monitor-starter/src/styles/brand.scss` 或 `pages/`
4. `apps/vue-support-payment-starter/src/main.ts` 的公共样式入口统一改为一个总入口，避免三连 import

### 第三组：需要明确为“生成产物”的样式

1. `pages/doc/dist-standalone/doc-v2-style.css`
   - 归档为构建产物
   - 不参与 workspace 公共源码统计
   - 从源码目录剥离或加入明确忽略策略
2. monitor 下大体积第三方样式：
   - `src/plugins/luckysheet/luckysheet.css`
   - `src/plugins/luckysheet/plugins.css`
   - `src/plugins/luckysheet/pluginsCss.css`
   - `src/plugins/luckysheet/iconfont.css`

这类文件不一定删除，但必须：

- 标记为第三方 vendor CSS
- 独立 chunk
- 仅在相关页面懒加载

### 第四组：standalone 组件瘦身

1. `ScMap` 只保留一个真实源码目录
2. `ScLayer` 只保留一个真实源码目录
3. `packages/standalone/*` 改成纯 re-export 壳，不能再复制样式文件
4. `packages/components/*` 中若只是桥接，也不要再保留独立样式副本

---

## 6. turbo / workspace 体积优化建议

### 6.1 样式入口去中心化，减少“全量牵连”

当前问题不是 turbo 自动把所有样式都打进一个包，而是：

- workspace 别名和全局 additionalData 把很多 app 都耦合到 `@layout/default`
- 公共层和布局层未分离，导致 layout 任意变更都会影响大量 app

建议：

1. `standard-config.ts` 的 `additionalData` 改为引用 `@repo/assets/styles/tokens/index.scss` 和 `@repo/assets/styles/mixins/index.scss`
2. 禁止默认注入 `@layout/default/styles/*`
3. layout 样式只由 layout 包自己入口显式引入
4. app 如果不用 layout，就不应该感知 layout token

### 6.2 建立“公共基础入口”和“按需入口”

建议公共入口只保留两级：

- `@repo/assets/styles/base/index.scss`：reset / tailwind bridge / html/body 基础层
- `@repo/assets/styles/theme/index.scss`：公共主题层

应用入口示例：

```ts
import "@repo/assets/styles/base/index.scss";
import "@repo/assets/styles/theme/index.scss";
import "./styles/app.scss";
```

不要再散落：

- `reset.scss`
- `tailwind.css`
- `index.scss`

分别在多个 app 手写组合。

### 6.3 大组件继续走 standalone 思路

你提到“像 `components-standalone` 那样单独体积”，我的理解是：

- 大组件必须有独立包边界
- 它自己的样式、依赖、第三方库不能混到 `@repo/components` 总入口里
- 页面只 import 某个 standalone 组件时，只带入该组件的 CSS 与依赖

建议：

1. `ScMap / ScLayer / ScCodeEditor / ScReteEditor / ScWebLLM / TechUI` 继续保留独立包边界
2. 但源码只保留一份，其他目录只做 re-export
3. 独立组件内部样式入口单独维护 `style-entry.ts`
4. app 层对重组件采用路由级或组件级动态导入

### 6.4 第三方 CSS 必须懒加载

优先处理：

- luckysheet
- doc-standalone
- map/editor/3D 类大组件配套 CSS

原则：

- 不进全局入口
- 不进 layout 入口
- 只在用到的页面 `import()` 或单组件局部引入

---

## 7. 推荐实施顺序

### Phase 0：冻结基线

- [x] 打 tag：`frontend-20260413-205712`
- [ ] 补一份样式/包体积基线报表（每个 app build 后 dist 体积）

### Phase 1：统一公共样式源

- [ ] 明确 `packages/assets` 为唯一公共样式源
- [ ] 决定只保留 `styles/` 或只保留 `style/`，二选一，不再双轨
- [ ] 将 `build-config` 的 additionalData 改到 `@repo/assets`
- [ ] 增加统一入口 `base/index.scss`、`theme/index.scss`

### Phase 2：删除 layout 中的公共副本

- [ ] 删除 `layout/default/src/styles/colors/*` 重复副本
- [ ] 删除 `layout/default/src/styles/layout/*` 中与 assets 重复的公共文件
- [ ] 删除 `layout/default/src/styles/theme-skin/*` 重复副本
- [ ] 保留 layout 自身专属样式，并改为从 assets 消费 token/mixin

### Phase 3：清洗 apps 重复样式

- [ ] payment/sync/swagger/hotspot 公共 mixin 收敛到 assets
- [ ] monitor 的共性 mixin 抽公共，特性 mixin 本地保留
- [ ] 所有 app 统一 `src/styles/app.scss` 入口
- [ ] 所有 app 删除本地复制版 `variables.scss` / `mixins.scss`

### Phase 4：清洗 standalone 重复目录

- [ ] ScMap / ScLayer / TechUI / ScCodeEditor / ScWebLLM / ScReteEditor 收敛成单源码目录
- [ ] 其余目录仅保留 re-export
- [ ] 校验别名解析是否仍然满足 `@repo/components/ScXxx`

### Phase 5：按需加载大 CSS

- [ ] monitor luckysheet CSS 懒加载
- [ ] doc standalone CSS 从源码区剥离
- [ ] map/editor/3D 组件 CSS 延迟加载

---

## 8. 验收标准

### 结构验收

- [ ] 公共样式只存在一份真实源码
- [ ] layout 不再承载公共 token/mixin/reset
- [ ] app 不再复制公共 variables/mixins
- [ ] standalone 组件不再多目录复制样式

### 构建验收

- [ ] 修改某个 app 本地样式，不再牵动 layout 公共注入链
- [ ] 修改 layout 皮肤，不影响非 layout app 的公共样式编译
- [ ] 重组件页面首次进入前，不提前加载对应 vendor CSS

### 体积验收

- [ ] 全局入口 CSS 数量减少
- [ ] app 主包 CSS 降低
- [ ] 第三方 CSS 改为异步 chunk
- [ ] turbo 增量构建命中率提升，变更影响面变小

---

## 9. 我建议你接下来这样做

### 先做的

1. 先统一 `packages/assets` 为唯一公共源
2. 再改 `build-config` 的 additionalData 指向 `@repo/assets`
3. 然后删 `layout/default/src/styles` 里的重复副本
4. 再处理各 app 的 `mixins.scss / variables.scss`
5. 最后处理 standalone 大组件和第三方大 CSS 懒加载

### 不建议一上来就做的

1. 不建议先全仓大改类名
2. 不建议先动所有页面视觉
3. 不建议先做“全部主题重写”
4. 不建议在公共层未稳定前去做大量 scoped/unscoped 迁移

---

## 10. 一句话总结

当前不是“样式太多”本身的问题，而是：

- 公共层、布局层、应用层没有真正分层
- 同一套样式在 layout / assets / app / standalone 里反复出现
- turbo/workspace 感知到的是一个高耦合样式网

正确方向是：

**把 `packages/assets` 做成唯一公共样式源，把 `layout` 收缩成框架壳，把 `apps` 收缩成业务皮肤，把大组件做成真正按需独立。**
