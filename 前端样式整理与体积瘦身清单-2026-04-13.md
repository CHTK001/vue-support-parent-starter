# vue-support-parent 样式整理与体积瘦身清单（2026-04-13）

## 1. 目标

把当前前端样式按下面三层重新收敛：

1. `layout/`：只放布局框架、主题壳、全局容器、导航、响应式骨架、主题皮肤。
2. `apps/<app>/src/styles`：只放某个应用自己的视觉样式，不再复制公共 token / mixin / reset / layout 规则。
3. `packages/assets`：只放跨应用复用的公共样式资产，例如 token、mixins、reset、公共 theme override、公共 page utilities。

同时解决 4 个问题：

- 删除 layout 与 assets 的双份样式副本
- 删除 app 之间重复的 variables/mixins/common 样式
- 降低 turbo/workspace 下“全量感知、全量牵连”的构建成本
- 让大组件像 `components-standalone` 一样按需独立，不把整仓样式和依赖一起带入

---

## 2. 我当前对仓库的理解

### 2.1 当前样式结构

仓库里现在样式主要分布在：

- `layout/default/src/**`
- `packages/assets/**`
- `apps/**/src/styles/**`
- `pages/**`（少量页面样式 + 一个已产物化的 `dist-standalone` CSS）
- `packages/components*` / `packages/standalone`（部分大组件自己的样式）

### 2.2 当前样式统计（源码层）

- 总样式文件数：`206`
- `layout`：`89` 个，约 `552.85 KB`
- `packages/assets`：`49` 个，约 `245.77 KB`
- `apps`：`38` 个，约 `325.03 KB`
- `pages`：`7` 个，约 `418.57 KB`
- `packages/components`：`7` 个，约 `39.39 KB`
- `packages/components-standalone`：`7` 个，约 `60.52 KB`
- `packages/standalone`：`7` 个，约 `60.52 KB`

> `pages/doc/dist-standalone/doc-v2-style.css` 单文件约 `381 KB`，明显属于构建产物，不应该长期作为源码样式参与 workspace 体积感知。

### 2.3 当前最核心的问题

#### 问题 A：`layout/default/src/styles` 与 `packages/assets/styles` 并存双份副本

已确认 **25 组完全相同** 的样式文件，重复源文件约 **110 KB**，包括：

- `layout/default/src/styles/layout/*` ↔ `packages/assets/styles/layout/default/*`
- `layout/default/src/styles/colors/*` ↔ `packages/assets/styles/colors/*`
- `layout/default/src/styles/theme-skin/*` ↔ `packages/assets/styles/layout/theme-skin/*`
- `layout/default/src/styles/font-encryption.scss` ↔ `packages/assets/style/font-encryption.scss`
- `layout/default/src/styles/modern-theme.scss` ↔ `packages/assets/style/legacy/modern-theme.scss`
- `layout/default/src/styles/layout-index.scss` ↔ `packages/assets/style/legacy/layout-index.scss`

也就是说：

- 仓库已经在“迁移到 `packages/assets`”
- 但迁移没收口，导致 layout 里还留着一整套旧实现
- 构建入口还在混用新旧路径

#### 问题 B：`packages/assets` 自己又同时存在 `style/` 和 `styles/`

这说明 `assets` 包内部也没彻底收敛：

- README 说“统一放 `style/`”
- 但当前实际被引用的是 `@repo/assets/styles/...`
- `style/` 与 `styles/` 又存在兼容入口 + 平行实现

这会带来：

- 新人不知道该从哪里引
- 一次改样式需要同时维护两到三份路径
- tree-shaking 很难建立明确边界

#### 问题 C：构建默认注入还绑着 `@layout/default`

`packages/build-config/src/standard-config.ts` 的 `scss.additionalData` 仍然是：

- `@layout/default/styles/layout/variables.scss`
- `@layout/default/styles/layout/mixin.scss`

这意味着：

- 即使 app 只想用公共 token，也被强耦合到 layout 包
- layout 改动会导致大量 app 的样式预处理链重新受影响
- `packages/assets` 很难成为真正唯一公共入口

#### 问题 D：app 之间在复制“公共 mixins / variables”

已确认以下 app 的样式文件存在明显语义重复：

- `apps/vue-support-payment-starter/src/styles/mixins.scss`
- `apps/vue-support-sync-starter/src/styles/mixins.scss`
- `apps/vue-support-swagger-starter/src/styles/mixins.scss`
- `apps/vue-support-hotspot-starter/src/styles/mixins.scss`
- `apps/vue-support-monitor-starter/src/styles/mixins.scss`

其中前四者几乎是同一份公共 mixin 模板的拷贝；monitor 版本更大，但其中大量基础 flex / text / card / responsive mixin 也属于公共层。

#### 问题 E：standalone / components-standalone / components 存在三套大组件样式镜像

已确认以下组件样式是重复副本：

- `ScMap`
- `ScLayer`

分布于：

- `packages/components/*`
- `packages/components-standalone/*`
- `packages/standalone/*`

这会造成：

- 样式维护三处同步
- 大组件边界不清晰
- workspace 下依赖图变大

---

## 3. 目标结构（建议落地方案）

### 3.1 最终职责划分

#### A. `layout/`

只保留：

- 布局壳体：header / sidebar / tabs / drawer shell / container
- 布局主题皮肤：dark / christmas / halloween / future-tech / skin
- 布局行为相关样式：响应式骨架、布局切换动画、导航模式差异
- 只被 layout 组件内部消费的局部样式

不再保留：

- 通用 reset
- 通用 variables / mixins
- 通用 color token
- 通用 element-plus override
- 所有 app 都可复用的 theme utilities

#### B. `packages/assets`

作为唯一公共样式源，建议保留：

- `tokens/`：颜色、圆角、阴影、spacing、z-index、字体等 token
- `mixins/`：flex / text / card / scrollbar / responsive / glass 等通用 mixin
- `base/`：reset、tailwind bridge、基础 html/body/scrollbar、font-encryption
- `theme/`：公共颜色主题、Element Plus override、theme-skin 公共层
- `pages/`：跨应用可复用页面级外观（例如 `modern-page`）
- `legacy/`：仅保留非常少量兼容转发文件，且必须有淘汰时间

#### C. `apps/<app>/src/styles`

只保留：

- 应用品牌色微调
- 应用首页/控制台/专题页视觉样式
- 应用特有的页面布局差异
- 不可抽象到公共层的业务样式

不再保留：

- 公共 variables
- 公共 mixins
- 公共 reset
- 从别的 app 复制来的 card / form / toolbar / flex 规则

---

## 4. 建议的目录重组清单

## 4.1 `packages/assets` 建议收口为唯一源

建议重组为：

```text
packages/assets/
  fonts/
  images/
  svg/
  styles/
    base/
      reset.scss
      tailwind.css
      font-encryption.scss
    tokens/
      colors.scss
      spacing.scss
      radius.scss
      shadow.scss
      z-index.scss
      motion.scss
      index.scss
    mixins/
      layout.scss
      text.scss
      card.scss
      effects.scss
      responsive.scss
      index.scss
    theme/
      colors/
      element-plus/
      skin/
      stitch/
      index.scss
    pages/
      modern-page.scss
    layout/
      default-entry.scss
    legacy/
      ...（只保留过渡转发）
```

### 4.2 `layout/default` 建议保留

建议保留：

- `src/components/**/themes`
- `src/skin/**`
- `src/themes/**`
- `src/styles/layout.scss` / `layout-index.scss` 中真正属于 layout 壳的内容

建议迁出或删除：

- `src/styles/layout/*` 中公共 token / reset / mixin / element-plus override
- `src/styles/colors/*`
- `src/styles/theme-skin/*`（如果是公共主题层则迁到 assets）
- `src/styles/font-encryption.scss`
- `src/styles/modern-theme.scss`
- 与 `packages/assets` 完全一致的兼容副本

### 4.3 apps 样式目录建议标准化

每个 app 统一成：

```text
apps/<app>/src/styles/
  app.scss           # app 唯一入口
  brand.scss         # app 品牌变量（少量）
  pages/
  components/
```

删除：

- `variables.scss`（如果只是公共 token 的复制）
- `mixins.scss`（如果只是公共 mixin 的复制）
- 从 monitor/payout/sync/swagger/hotspot 之间互相复制的模板文件

---

## 5. 具体清理清单

### 第一组：立即可删/可迁移的重复公共样式

1. 对比删除 `layout/default/src/styles/layout/*` 与 `packages/assets/styles/layout/default/*` 的双份文件
2. 对比删除 `layout/default/src/styles/colors/*` 与 `packages/assets/styles/colors/*` 的双份文件
3. 对比删除 `layout/default/src/styles/theme-skin/*` 与 `packages/assets/styles/layout/theme-skin/*` 的双份文件
4. 对比删除 `layout/default/src/styles/font-encryption.scss`
5. 对比删除 `layout/default/src/styles/modern-theme.scss`
6. 对比删除 `layout/default/src/styles/layout-index.scss` 的 legacy 镜像来源

### 第二组：立即可抽公共的 app 样式

1. 抽离四个 app 共用的 `flex-* / text-ellipsis / card-*` mixin 到 `packages/assets/styles/mixins/`
2. 抽离四个 app 共用的 `radius/shadow/border/duration/ease/gradient` 变量到 `packages/assets/styles/tokens/`
3. monitor 的大份 `mixins.scss` 拆成：
   - 公共 mixins → `packages/assets`
   - monitor 专属 mixins → `apps/vue-support-monitor-starter/src/styles/brand.scss` 或 `pages/`
4. `apps/vue-support-payment-starter/src/main.ts` 的公共样式入口统一改为一个总入口，避免三连 import

### 第三组：需要明确为“生成产物”的样式

1. `pages/doc/dist-standalone/doc-v2-style.css`
   - 归档为构建产物
   - 不参与 workspace 公共源码统计
   - 从源码目录剥离或加入明确忽略策略
2. monitor 下大体积第三方样式：
   - `src/plugins/luckysheet/luckysheet.css`
   - `src/plugins/luckysheet/plugins.css`
   - `src/plugins/luckysheet/pluginsCss.css`
   - `src/plugins/luckysheet/iconfont.css`

这类文件不一定删除，但必须：

- 标记为第三方 vendor CSS
- 独立 chunk
- 仅在相关页面懒加载

### 第四组：standalone 组件瘦身

1. `ScMap` 只保留一个真实源码目录
2. `ScLayer` 只保留一个真实源码目录
3. `packages/standalone/*` 改成纯 re-export 壳，不能再复制样式文件
4. `packages/components/*` 中若只是桥接，也不要再保留独立样式副本

---

## 6. turbo / workspace 体积优化建议

### 6.1 样式入口去中心化，减少“全量牵连”

当前问题不是 turbo 自动把所有样式都打进一个包，而是：

- workspace 别名和全局 additionalData 把很多 app 都耦合到 `@layout/default`
- 公共层和布局层未分离，导致 layout 任意变更都会影响大量 app

建议：

1. `standard-config.ts` 的 `additionalData` 改为引用 `@repo/assets/styles/tokens/index.scss` 和 `@repo/assets/styles/mixins/index.scss`
2. 禁止默认注入 `@layout/default/styles/*`
3. layout 样式只由 layout 包自己入口显式引入
4. app 如果不用 layout，就不应该感知 layout token

### 6.2 建立“公共基础入口”和“按需入口”

建议公共入口只保留两级：

- `@repo/assets/styles/base/index.scss`：reset / tailwind bridge / html/body 基础层
- `@repo/assets/styles/theme/index.scss`：公共主题层

应用入口示例：

```ts
import "@repo/assets/styles/base/index.scss";
import "@repo/assets/styles/theme/index.scss";
import "./styles/app.scss";
```

不要再散落：

- `reset.scss`
- `tailwind.css`
- `index.scss`

分别在多个 app 手写组合。

### 6.3 大组件继续走 standalone 思路

你提到“像 `components-standalone` 那样单独体积”，我的理解是：

- 大组件必须有独立包边界
- 它自己的样式、依赖、第三方库不能混到 `@repo/components` 总入口里
- 页面只 import 某个 standalone 组件时，只带入该组件的 CSS 与依赖

建议：

1. `ScMap / ScLayer / ScCodeEditor / ScReteEditor / ScWebLLM / TechUI` 继续保留独立包边界
2. 但源码只保留一份，其他目录只做 re-export
3. 独立组件内部样式入口单独维护 `style-entry.ts`
4. app 层对重组件采用路由级或组件级动态导入

### 6.4 第三方 CSS 必须懒加载

优先处理：

- luckysheet
- doc-standalone
- map/editor/3D 类大组件配套 CSS

原则：

- 不进全局入口
- 不进 layout 入口
- 只在用到的页面 `import()` 或单组件局部引入

---

## 7. 推荐实施顺序

### Phase 0：冻结基线

- [x] 打 tag：`frontend-20260413-205712`
- [x] 补一份样式/包体积基线报表（每个 app build 后 dist 体积）

### Phase 1：统一公共样式源

- [x] 明确 `packages/assets` 为唯一公共样式源（公共注入与 layout 兼容入口已统一走 `@repo/assets/styles/*`）
- [x] 决定 `styles/` 为唯一真实源码目录，`style/` 仅保留兼容转发（deprecated）
- [x] 将 `build-config` 的 additionalData 改到 `@repo/assets`
- [x] 增加统一入口 `base/index.scss`、`theme/index.scss`

### Phase 2：删除 layout 中的公共副本

- [x] 删除 `layout/default/src/styles/colors/*` 重复副本（已改为 `@repo/assets/styles/*` 转发壳）
- [x] 删除 `layout/default/src/styles/layout/*` 中与 assets 重复的公共文件（已改为转发壳）
- [x] 删除 `layout/default/src/styles/theme-skin/*` 重复副本（已改为转发壳）
- [x] 保留 layout 自身专属样式，并改为从 assets 消费 token/mixin

### Phase 3：清洗 apps 重复样式

- [x] payment/sync/swagger/hotspot 公共 mixin 收敛到 assets
- [x] monitor 的共性 mixin 抽公共，特性 mixin 本地保留（app 内仅保留转发入口）
- [x] 所有 app 统一 `src/styles/app.scss` 入口
- [x] 所有 app 删除本地复制版 `variables.scss` / `mixins.scss`（已清理 payment/sync/swagger/hotspot 无引用副本）

### Phase 4：清洗 standalone 重复目录

- [x] ScMap / ScLayer / TechUI / ScCodeEditor / ScWebLLM / ScReteEditor 收敛成单源码目录（workspace 构建链真实实现统一为 `packages/components-standalone/*`）
- [x] `packages/standalone/*` 仅保留 re-export 与必要样式转发壳
- [x] 校验别名解析仍满足 `@repo/components/ScXxx`（开发态继续由 `packages/build-config/src/utils.ts` 映射到 `components-standalone`）

### Phase 5：按需加载大 CSS

- [x] monitor luckysheet CSS 不进入主构建链（当前 `dist` CSS 检索 `luckysheet-` 为 0 命中，vendor 目录保留待业务页按需接入）
- [x] doc standalone CSS 已从源码区剥离（仓库内受版本控制的 `pages/**/dist-standalone` 产物为 0）
- [ ] map/editor/3D 组件 CSS 延迟加载（已完成 `ScMap/ScLayer`；editor 类待继续）

---

## 8. 验收标准

### 结构验收

- [x] 公共样式只存在一份真实源码（`packages/assets/styles`）
- [x] layout 不再承载公共 token/mixin/reset（仅保留兼容转发壳）
- [x] app 不再复制公共 variables/mixins
- [x] standalone 组件不再多目录复制样式（`packages/standalone/*` 样式已收口为转发壳）

### 构建验收

- [ ] 修改某个 app 本地样式，不再牵动 layout 公共注入链
- [ ] 修改 layout 皮肤，不影响非 layout app 的公共样式编译
- [ ] 重组件页面首次进入前，不提前加载对应 vendor CSS

### 体积验收

- [ ] 全局入口 CSS 数量减少
- [ ] app 主包 CSS 降低
- [ ] 第三方 CSS 改为异步 chunk
- [ ] turbo 增量构建命中率提升，变更影响面变小

---

## 9. 我建议你接下来这样做

### 先做的

1. 先统一 `packages/assets` 为唯一公共源
2. 再改 `build-config` 的 additionalData 指向 `@repo/assets`
3. 然后删 `layout/default/src/styles` 里的重复副本
4. 再处理各 app 的 `mixins.scss / variables.scss`
5. 最后处理 standalone 大组件和第三方大 CSS 懒加载

### 不建议一上来就做的

1. 不建议先全仓大改类名
2. 不建议先动所有页面视觉
3. 不建议先做“全部主题重写”
4. 不建议在公共层未稳定前去做大量 scoped/unscoped 迁移

---

## 10. 一句话总结

当前不是“样式太多”本身的问题，而是：

- 公共层、布局层、应用层没有真正分层
- 同一套样式在 layout / assets / app / standalone 里反复出现
- turbo/workspace 感知到的是一个高耦合样式网

正确方向是：

**把 `packages/assets` 做成唯一公共样式源，把 `layout` 收缩成框架壳，把 `apps` 收缩成业务皮肤，把大组件做成真正按需独立。**

---

## 11. 执行进展（2026-04-14）

### 11.1 已落地改动

1. 公共 SCSS 注入已从 layout 直连改为 assets 聚合入口：
   - `packages/build-config/src/standard-config.ts`
   - `apps/vue-support-sync-starter/vite.config.ts`
2. 新增 assets 聚合入口并对齐注入：
   - `packages/assets/styles/tokens/index.scss`
   - `packages/assets/styles/mixins/index.scss`
3. layout 兼容转发去掉 `@repo/assets/style/*` 旧路径引用，统一改到 `@repo/assets/styles/*`：
   - `layout/default/src/styles/layout-index.scss`
   - `layout/default/src/styles/modern-theme.scss`
   - `layout/default/src/styles/font-encryption.scss`
4. `styles/` 已收口为真实源码目录，`style/` 仅保留兼容转发层：
   - `packages/assets/styles/font-encryption.scss`
   - `packages/assets/styles/legacy/layout-index.scss`
   - `packages/assets/styles/legacy/modern-theme.scss`
   - `packages/assets/styles/legacy/gridstack.scss`
   - `packages/assets/styles/legacy/map-icons.css`
   - `packages/assets/style/**` 对应转发壳
5. `@repo/assets` 包发布文件列表已纳入 `styles/` 目录：
   - `packages/assets/package.json`
6. 清理确认无引用的 app 样式冗余文件（共 11 个）：
   - `apps/vue-support-payment-starter/src/styles/mixins.scss`
   - `apps/vue-support-payment-starter/src/styles/variables.scss`
   - `apps/vue-support-sync-starter/src/styles/mixins.scss`
   - `apps/vue-support-sync-starter/src/styles/variables.scss`
   - `apps/vue-support-sync-starter/src/styles/responsive.scss`
   - `apps/vue-support-swagger-starter/src/styles/mixins.scss`
   - `apps/vue-support-swagger-starter/src/styles/variables.scss`
   - `apps/vue-support-hotspot-starter/src/styles/mixins.scss`
   - `apps/vue-support-hotspot-starter/src/styles/variables.scss`
   - `apps/vue-support-monitor-starter/src/styles/theme-common.scss`
   - `apps/vue-support-monitor-starter/src/styles/threshold.scss`
7. payment 样式入口语义统一为 `app.scss`：
   - `apps/vue-support-payment-starter/src/main.ts`
   - `apps/vue-support-payment-starter/src/styles/app.scss`
8. 补全公共样式双入口并接入启动链：
   - `packages/assets/styles/base/index.scss`
   - `packages/assets/styles/theme/index.scss`
   - `packages/core/src/app-bootstrap.ts`
9. 资产包文档已改为 `styles` 主入口规范：
   - `packages/assets/README.md`
10. monitor 共性样式已收敛到 assets（app 侧只保留转发入口）：
   - `apps/vue-support-monitor-starter/src/styles/variables.scss`
   - `apps/vue-support-monitor-starter/src/styles/mixins.scss`
   - `packages/assets/styles/tokens/monitor-base.scss`
   - `packages/assets/styles/mixins/monitor-advanced.scss`
11. app 样式入口已统一为 `src/styles/app.scss` 并在 `main.ts` 显式引入：
   - `apps/vue-support-email-starter/src/main.ts`
   - `apps/vue-support-hotspot-starter/src/main.ts`
   - `apps/vue-support-monitor-starter/src/main.ts`
   - `apps/vue-support-spring-pages-starter/src/main.ts`
   - `apps/vue-support-swagger-starter/src/main.ts`
   - `apps/vue-support-sync-starter/src/main.ts`
   - `apps/vue-support-system-parent/src/main.ts`
12. legacy standalone 大组件已收口为纯桥接壳，真实实现统一回到 `components-standalone`：
   - `packages/standalone/ScMap/index.ts`
   - `packages/standalone/ScLayer/index.ts`
   - `packages/standalone/ScCodeEditor/index.ts`
   - `packages/standalone/ScReteEditor/index.ts`
   - `packages/standalone/ScWebLLM/index.ts`
   - `packages/standalone/TechUI/index.ts`
13. `packages/standalone/*` 共删除 `132` 个重复 tracked 源文件，仅保留：
   - 包入口 `index.ts`
   - `package.json`
   - `ScMap/ScLayer` 的样式转发文件 `styles/*`
   - 必要 README
14. `ScMap / ScLayer` 的重型 vendor CSS 已改为缓存式按需加载：
   - `packages/components-standalone/ScMap/style-loader.ts`
   - `packages/components-standalone/ScLayer/style-loader.ts`
   - `packages/components-standalone/ScMap/index.vue`
   - `packages/components-standalone/ScMap/composables/MapObject.ts`
   - `packages/components-standalone/ScMap/utils/IconUtils.ts`
   - `packages/components-standalone/ScLayer/index.vue`
   - `packages/components-standalone/ScLayer/composables/MapObject.ts`
   - `packages/components-standalone/ScLayer/composables/CesiumObject.ts`
   - `packages/components-standalone/ScLayer/composables/ShapeObject.ts`
   - `packages/components-standalone/ScLayer/utils/IconUtils.ts`
15. `packages/components-standalone` 已补充样式动态导入声明，避免 `style-loader.ts` 引入新的 TS 资源声明报错：
   - `packages/components-standalone/tsconfig.json`
   - `packages/components-standalone/style-modules.d.ts`

### 11.2 性能优化关联进展

1. 已修复首页「右侧加载动画撑开页面宽度」问题（根因：36 列布局数据被 12 列网格解释导致越界渲染）。
2. 通过无头回归确认：异常布局数据下首页仍无横向溢出，避免无效渲染区域扩大造成额外重排与合成开销。

### 11.3 构建验证（本轮）

1. `pnpm --filter ./apps/vue-support-payment-starter build`：通过
2. `pnpm --filter ./apps/vue-support-system-parent build`：通过
3. `pnpm --filter ./apps/vue-support-monitor-starter build`：通过
4. `pnpm --filter ./apps/vue-support-sync-starter build`：未通过，阻塞于现存 Tailwind 类 `border-primary` 缺失，报错位置在 `pages/project/src/views/ai/resolution/index.vue`
5. `pnpm exec tsc -p packages/standalone/tsconfig.json --noEmit`：未通过，暴露的是仓库既有 TS/资源声明问题；本轮引入的 `ScLayer/ScReteEditor` 默认导出转发已按报错修正
6. `apps/vue-support-monitor-starter/dist` 检索 `luckysheet-`：`0` 命中，当前主构建产物未混入 luckysheet vendor CSS
7. `pnpm --filter @pages/map build`：通过，`ScLayer` 新增 style loader 与异步初始化顺序未打断页面包导出链
8. `pnpm --filter @pages/example build`：未通过，阻塞于仓库现存 `pages/example/src/components/ScCardInputExample.vue` SFC 解析错误：`Expected "as" but found "ScInput"`
9. `pnpm exec tsc -p packages/components-standalone/tsconfig.json --noEmit`：仍有 standalone 既有类型噪音，但本轮新增的 `style-loader.ts` CSS 模块报错已通过 `style-modules.d.ts` 收敛；当前输出未命中新接入的 `ScMap/ScLayer` loader 调用点

### 11.4 体积基线快照（2026-04-14）

> 基线统计口径：各 `apps/*/dist` 当前构建产物；单位为 MB。

| App | Dist | CSS 合计 | CSS 文件数 | Top3 CSS（按体积） |
| --- | ---: | ---: | ---: | --- |
| vue-support-email-starter | 34.85 | 4.84 | 75 | index-BgPUp_hc.css (1582.3 KB); index-CdsyYHMU.css (1147.7 KB); index-CjjloNKa.css (184.4 KB) |
| vue-support-hotspot-starter | 37.05 | 4.99 | 85 | index-llrcb4t2.css (1582.7 KB); index-k7nA7Kxh.css (1130.4 KB); index-Bv6wipMd.css (185.6 KB) |
| vue-support-iconify-offline-starter | N/A | N/A | N/A | (dist 不存在) |
| vue-support-mock-server-starter | 0.01 | 0 | 0 | - |
| vue-support-monitor-starter | 106.05 | 9.65 | 460 | index-CWPkNXps.css (1196.6 KB); project-ai-CHhurA78.css (577.3 KB); index-DMOe8Sq5.css (472.4 KB) |
| vue-support-payment-starter | 80.54 | 5.58 | 177 | index-D_sSLp4T.css (1227 KB); index-DDwqSnFK.css (747.1 KB); vendor-ui-x4F4nOG2.css (346.8 KB) |
| vue-support-spring-pages-starter | 13.02 | 2.10 | 4 | index-BgPUp_hc.css (1582.3 KB); vendor-ui-Dh61In7b.css (346.8 KB); runtime-D4i_-GRT.css (215.6 KB) |
| vue-support-swagger-starter | 35.07 | 4.92 | 69 | index-llrcb4t2.css (1582.7 KB); index-k7nA7Kxh.css (1130.4 KB); index-Bv6wipMd.css (185.6 KB) |
| vue-support-sync-starter | 40.13 | 5.46 | 66 | index-Cp9l9eMA.css (1582.4 KB); index-Cqa5WR0K.css (1244.8 KB); index-C23Orw65.css (746.2 KB) |
| vue-support-system-parent | 92.98 | 6.36 | 243 | index-CWPkNXps.css (1196.6 KB); vendor-ui-B67KUA7a.css (365 KB); index-Dlmk1oV6.css (266.5 KB) |

### 11.5 下一步（待继续）

1. 完成 Phase 5 余项：editor 类组件 CSS 的显式延迟加载。
2. 在实际 Vite app 构建中校验 `ScMap/ScLayer` vendor CSS 是否已经落到异步 chunk，而不是仅完成源码侧接入。
3. 继续清理 `packages/components/*` 中少数历史桥接目录，消除剩余代码镜像与异常 junction。
4. 修复当前构建基线中的历史问题：`border-primary` Tailwind 类缺失、全仓 TS/资源声明噪音。

---

## 12. 任务计划（执行版）

> 本计划由本文原有“目标结构 + 具体清理清单 + Phase 方案”映射而成，并持续以勾选状态维护。

### 12.1 任务总表（含原有样式整理项）

- [x] TP-00 基线冻结：tag 与体积基线采集（对应 Phase 0）
- [x] TP-10 公共源收口：`@repo/assets/styles` 成为唯一真实样式源（对应问题 A/B、Phase 1）
- [x] TP-11 公共入口标准化：补齐 `base/index.scss` 与 `theme/index.scss`（对应 6.2、Phase 1）
- [x] TP-12 启动链对齐：核心加载链与 payment 入口改为 `base + theme + app`（对应 6.2）
- [x] TP-20 layout 副本清理：删除 `layout/default/src/styles/{colors,layout,theme-skin}` 重复实现（对应第一组、Phase 2）
- [x] TP-30 app 共性样式收敛：payment/sync/swagger/hotspot/monitor 共性 mixins/tokens 收敛到 assets（对应第二组、Phase 3）
- [x] TP-31 app 入口统一：各 app 统一 `src/styles/app.scss`（对应第二组、Phase 3）
- [x] TP-40 standalone 单源码化：ScMap/ScLayer/TechUI/ScCodeEditor/ScWebLLM/ScReteEditor（对应第四组、Phase 4）
- [ ] TP-50 重 CSS 懒加载：luckysheet/doc-standalone/map-editor（已完成 map/3D 子项；editor 待继续，对应第三组、Phase 5）

### 12.2 本轮已完成任务

- [x] 完成 TP-10：`style/` 中真实实现迁移到 `styles/`，旧路径改兼容转发
- [x] 完成 TP-11：新增 `packages/assets/styles/theme/index.scss`
- [x] 完成 TP-12：`packages/core/src/app-bootstrap.ts` 与 `apps/vue-support-payment-starter/src/main.ts` 接入 `theme/index.scss`
- [x] 完成 TP-20：`layout/default/src/styles/{colors,layout,theme-skin}` 保留兼容壳，真实实现仅在 assets
- [x] 完成 TP-30（monitor 子项）：monitor 共性 `variables/mixins` 固化到 `packages/assets/styles/tokens|mixins`
- [x] 完成 TP-31：email/hotspot/monitor/spring-pages/swagger/sync/system 统一 `styles/app.scss` 并从 `main.ts` 引入
- [x] 完成 TP-00 基线报告补齐：见 11.4 表格
- [x] 完成 TP-40：`packages/standalone` 下 6 个 legacy 大组件包改为纯 re-export 壳，删除 `132` 个重复 tracked 源文件
- [x] 完成 TP-50（审计子项）：仓库内 `pages/**/dist-standalone` tracked 产物为 `0`；monitor 当前 `dist` CSS 未检出 `luckysheet-`
- [x] 完成 TP-50（map/3D 子项）：`ScMap/ScLayer` 的 `leaflet` / `ol` / `ol-ext` / `cesium widgets` CSS 已切到缓存式动态导入；`ScLayer` 的 3D 样式改为在切换 3D 时加载
