| 编号 | 模块                      | 问题描述                                                           | 影响                             | 状态   | 备注                           |
| ---- | ------------------------- | ------------------------------------------------------------------ | -------------------------------- | ------ | ------------------------------ |
| D-01 | vue-support-monitor-starter | `v-menu` / `v-ripple` 指令报 `Failed to resolve directive`，未正确注册 | 头部工具栏、用户下拉等功能异常   | 待修复 | 需从 `@repo/core/directives` 导入 |
| D-02 | layout/default/BaseSetting | 模板中访问 `themeStore.*`，但组件实例上未定义 `themeStore`        | 渲染时报错，可能触发重复渲染     | 待修复 | 需要在 `<script setup>` 中创建 |
| D-03 | vue-tippy / BaseSetting   | 控制台多次输出 `onMounted is called when there is no active component instance` 警告 | 控制台刷屏，可能伴随性能下降     | 观察中 | 待确认在修复 D-01/D-02 后是否仍存在 |


